config {
    type: "table",
    schema: "golf_transform",
    bigquery: {
        clusterBy: ["practice_date"]
    }
}

SELECT
  practice_date,
  club_duplicate,
  club_sort,
  shot_number,
  ball_speed_mph,
  launch_angle
FROM
  ${ref("cleaned_duplicate")}

