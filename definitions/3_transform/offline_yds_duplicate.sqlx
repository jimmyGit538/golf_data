config {
    type: "table",
    schema: "golf_transform",
    bigquery: {
        clusterBy: ["practice_date"]
    }
}

SELECT
  practice_date,
  club_duplicate,
  club_sort,
  shot_number,
  offline_yds,
  carry_yds
FROM
  ${ref("cleaned_duplicate")}
  GROUP BY ALL

